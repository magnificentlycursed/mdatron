# Changelog

All notable changes to mdatron will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

mdatron is descended from Schematron (ISO/IEC 19757-3). It is **not** related to
the TRON blockchain.

## [Unreleased]

### Added
- input resource bounds are enforced with a loud diagnostic, and the `verify --json` envelope's `mdatron_output_version` moves **2.0.0 → 2.1.0** to carry the new failure kind (#124, roast SHO1): a per-file byte cap (`MAX_FILE_BYTES`, 8 MiB), an aggregate-snapshot byte cap (`MAX_AGGREGATE_BYTES`, 64 MiB), and a structural-nesting cap (`MAX_STRUCTURAL_NESTING`, 256) each exceedance a `pipeline_error` with the new `kind: "bound_exceeded"`; the DSL expression-nesting cap (`MAX_EXPR_DEPTH`, 256) surfaces as `kind: "expr_parse"` (a bounded `ParseError`, not the stack-overflow abort it prevents). Together these replace silent degradation, unbounded memory, and an uncatchable parser abort. The `bound_exceeded` value is **additive** to the closed `pipeline_error.kind` enum, so the envelope moves by a **MINOR** (`2.0.0 → 2.1.0`) per the versioned-contract SemVer rule (§ Diagnostics are a versioned contract); the published schema, `OUTPUT_VERSION`, and `E0080`'s explain page are updated in lockstep. (This entry documents a contract change that shipped after the 0.4.0 tag but was previously unrecorded — a gap the pre-publish re-roast caught, D1; the expr-depth `kind` was itself corrected in round 2.)
- `mdatron verify --deny-warnings` (alias `--strict`) escalates a warnings-only run to a failing exit (#121, requested by vsdd-cli mdatron#1): an otherwise-clean run (exit `0`) that carries warnings now exits `1`, so a hard-gate consumer can fail on warnings without parsing the JSON envelope. Errors (`1`), pipeline failures (`2`), and a genuinely clean run (`0`) are unchanged; the escalation is purely `0 → 1` when `summary.warning_count > 0`.

### Changed
- the published output schema's `$id` moves from `https://mdatron.dev/...` (an unowned domain) to `https://github.com/magnificentlycursed/mdatron/schema/mdatron-output/<version>` — the project's own GitHub namespace (#131 SO ruling / DEF6): a stable, version-unique identifier that is not a dangling/squattable reference. The `$id` remains an identifier, not a fetch target (the resolvable copy is the repo's `schema/mdatron-output.schema.json`, surfaced by `mdatron schema`). Not a version bump: `mdatron_output_version` and the envelope shape are unchanged, and `mdatron.dev` never resolved to a document, so no consumer could have been fetching it.
- `verify --compact` now truncates message prose on a **word boundary** rather than mid-word (#119, vsdd-cli v0.3.0 diagnostic-efficacy review W4 polish): when the 512-byte compact cap forces the lower-priority engine message to be cut, it retreats to the last interior whitespace so the tail reads `… — the quick…` instead of `… — the quick bro…`, and trailing whitespace before the ellipsis is trimmed. A cut already at a word gap keeps its final word; a single token wider than the whole budget has no interior space to retreat to and still takes the hard char-boundary cut (word-boundary is a preference, not a requirement). The 512-byte cap and the quoted-value-ahead-of-prose priority (#115) are unchanged.

### Fixed
- `pipeline_error.message` is now guaranteed host-layout-free for **every** load-path failure (#142, pre-publish re-roast round-3 A): round 2 relativized the specific messages it found, but round 3 showed the leak was broader — a malformed **config/pin/vocabulary/route** file produces a `cannot parse '<abs>'` `Config` message that `relativize_paths` cannot reach. A chokepoint in `cmd_verify` now strips any project-root prefix from the final `pipeline_error.message` (both the original and canonicalized roots, trailing-separator match), a completeness guarantee no per-source fix can make for future messages. Also (round-3 B): the schema `pattern` interpolated inline into an `E0050` message is now escaped (Cc ∪ Zl/Zp) via a shared `escape_inline` helper, closing the last raw-inline surface in `describe()` for partition parity with `at_pointer`/`diagnostic.rs`. Red gates: `pipeline_error_message_relativizes_a_parse_error_path`, `pattern_line_separators_escaped_in_message`.
- `pipeline_error.message` no longer leaks absolute host paths on the three most likely pipeline failures (#140, pre-publish re-roast round-2 A1/A2): the round-1 DEF4 completion (#134) relativized the `VerifyError` `{path}` variants but the path-carrying *free-form* messages still leaked — a **missing** `.mdatron/config.yaml` (the most common failure) and a **globless** config now name the config relatively at the source, a `glob` per-entry read error relativizes the failing path, and an index-build IO/parse error carries the root-relative source path. The JSON envelope's `pipeline_error.message` is now host-layout-free and cross-machine-reproducible across the common failure classes. (A round-2 review also corrected the `catastrophic_pattern_matches_in_linear_time` test's docstring — B2 — which overclaimed a linear-vs-backtracking discrimination the test does not make; the load-bearing linearity guard is the fancy-feature rejection test.)
- roast round-1 low-severity hardening (atomic writes + diagnostic rendering): `atomic::write` now opens its sidecar temp `O_EXCL` (`create_new`) with a retrying attempt counter, so a temp-name collision (two runs in one clock tick, a coarse-resolution clock) retries with a fresh name rather than silently truncating a colliding temp (A3); its module doc records that a Windows rename can fail loudly on a sharing violation with the target intact (B3) and that a crash-leftover temp is harmless (A2). In diagnostic rendering, `verify --compact` no longer emits a dangling `" — "` for a content-free (empty/all-whitespace) message (A4), and a quoted region's `label` is control-char-escaped (Cc ∪ Zl/Zp) in both the TTY and compact forms so a pattern-derived label cannot forge an unprefixed line (B4).
- DEF4 relativization now covers the three remaining envelope path-carriers a pre-publish re-roast found still leaking absolute host paths (#134, roast round-1 A1/B1/B2): `pipeline_error.message` on a failed pipeline (the `VerifyError` `{path}` variants are relativized against the canonicalized root before the message is built), the DSL `$file.path` (now project-root-relative, matching `location.file`), and the `E0010`/`E0011` confinement-escape findings (now anchored at `.mdatron/config.yaml` with the escaping path carried as prefix-marked adopter content, since an escaping path is by definition outside the root and cannot be relativized). The DEF4 invariant test is strengthened to exercise all three.
- every diagnostic `location.file` path in a `verify` run is now project-root-relative, closing a host-layout leak and a cross-machine reproducibility gap (#131 SO ruling / DEF4): walked-file findings were already relativized, but project-level findings (W0047 schemas dir, W0046 config glob, vocabulary) emitted absolute host paths (e.g. `/Users/you/proj/.mdatron/schemas`) via `project_root.join(...)`, leaking the host filesystem layout into the JSON envelope and making two machines' outputs non-diffable. A single normalization pass in `verify::run` strips the project root from every finding's path before the (now relative-keyed, equally reproducible) sort; it is a no-op on an already-relative path. This is impl compliance with the standing DESIGN contract (§ Agents are the first consumer: "file paths rendered relative to the governed tree root"), not a new envelope shape — no version bump.
- diagnostic-surface consistency fixes surfaced by the pre-publish roast (#126, SHO6/8/9/11): (SHO6) `verify --files` help no longer contradicts the shipped refuse-without-jurisdiction behavior; (SHO11) `verify --json` help cites the binary's own published schema rather than a dead `../vsdd-cli/` path; (SHO8) the `MDATRON-E0012` name is canonicalized to `symlinked-component-refused` so the explain page, the golden code-catalog, and the runtime `Finding.summary` all agree (the divergence previously passed CI); (SHO9) the `E0080` explain page and DESIGN § Init no longer overclaim what `mdatron init` scaffolds — corrected from "route/registry/pin directories" to the actual `schemas/`+`patterns/` dirs plus single-file families.
- `pins.yaml` and the managed-partition manifest are now written atomically (#126 DEF8, roast defer-bucket): both are rewritten in place (a `pin --update`, an `init` repair), and `std::fs::write` truncates the target before writing, so a crash, full disk, or `SIGKILL` mid-write left a half-written governance file. A new `atomic::write` helper writes to a same-directory temp file, fsyncs it, then renames it over the target — atomic on the same volume on Unix (`rename(2)`) and Windows (`MoveFileExW`+`MOVEFILE_REPLACE_EXISTING`) — so a reader observes either the whole old file or the whole new one, never a partial. All production `init`/`pin` writes route through it; a failed write leaves the target untouched and no temp residue.
- a declared `schema_class` that nothing serves is no longer silently clean under a **present-but-empty** `.mdatron/schemas/` directory (#111, answered by vsdd-cli mdatron#1): `MDATRON-W0045` (schema-class-unrouted) previously fired only when some validation infrastructure existed, so an init'd project whose `schemas/` and `patterns/` are both empty — a `schema_class` file whose `.json` was never added — exited `0` with no finding, the same silent-false-clean class W0045/W0047 exist to catch. W0045 now fires whenever the schemas directory is **present** (empty or not) and the declared class is unrouted; a **missing** directory remains the project-level `MDATRON-W0047`'s job, so the two never double-report the same file. Fresh-init-clean is preserved: a file with no `schema_class` declares no intent to be validated and stays silent. `verify_file` takes the dir-present signal; the `no_adopter_data_runs_with_all_families_inactive` invariant is updated (the declared-class file now warrants the warning, families still all inactive).

### Security
- the JSON pointer interpolated into a schema-validation (`E0050`) message now escapes the Unicode line/paragraph separators U+2028/U+2029, not just the C0/C1 controls (#140, pre-publish re-roast round-2 B1): a contributor-controlled frontmatter **key** carrying a raw U+2028 previously survived inline in the engine-authored message, forging an unprefixed line — regressing the #125 rendering partition that already hardened `diagnostic.rs`. `at_pointer` now matches the ratified partition (Cc ∪ {U+2028, U+2029}), closing the last inline-adopter-content break surface.
- CI now runs `cargo deny check bans licenses sources` over the full transitive dependency tree (#139, roast C4): a `deny.toml` allow-lists the permissive licenses actually present (MIT/Apache-2.0/BSD/Unicode-3.0/BSL-1.0/Zlib/Unlicense/MIT-0), denies unknown registries and git sources, and denies wildcard version requirements — so an incompatible license or an untrusted source in a transitive dependency fails CI rather than shipping silently in an MIT crate that vendors ~140 crates. The `cargo-deny` tool is version-pinned (#141, roast round-2 C1) so the closed license allow-list cannot float to a future release and red the gate with no repo change. Advisories remain with the existing `cargo audit` step (no double-check divergence).
- adopter JSON Schema `pattern`/`patternProperties` now validate on a **linear-time** regex engine, closing a ReDoS surface on the verify gate (#135, roast C1). `jsonschema` is upgraded `0.18 → 0.49` and built with `PatternOptions::regex()`, routing pattern matching through the linear `regex` crate instead of the backtracking `fancy-regex` — so a contributor cannot craft frontmatter that drives catastrophic backtracking against a project's schema pattern, and DESIGN.md's "linear-time engines" guarantee (§ Behavioral contracts) is now actually true for this surface (route/vocabulary already used linear `regex-lite`). **Behavior change:** look-around and backreferences (fancy-regex-only features, unsupported by a linear engine and not needed for frontmatter shape validation) are now **refused at schema compile**. The upgrade also expands the transitive dependency tree by ~18 crates (the 0.49 workspace modularization; see `docs/dependencies/jsonschema.md`); the API migration (`JSONSchema`→`Validator`, `compile`→`build`, `validate`→`iter_errors`, error accessors now methods) is internal. Proven by `linear_engine_refuses_backreference_pattern` and `catastrophic_pattern_matches_in_linear_time`.

## [0.4.0] - 2026-07-29

Ships the vsdd-cli v0.3.0 diagnostic-efficacy review (the "checked-N vs
checked-nothing" audit-signal work, #104). **Breaking**, per SemVer: the
`verify --json` envelope's `mdatron_output_version` moves **1.1.0 → 2.0.0** — a
MAJOR bump because, since the last released envelope (1.1.0, in 0.3.0), `families`
was reshaped from a string enum to a `{state, reason}` object (#107) and each
`quoted[]` region gained REQUIRED `origin`/`trusted` fields under a closed object
(#114); both are rejected by a 1.1.0-schema validator. `pipeline_error` (#112) is
additive. (Development passed through envelope 1.2.0–1.4.0, never released; the
release collapses them into the single honest major bump.) Accordingly the crate
moves `0.3.0 → 0.4.0` (a breaking change bumps the `0.MINOR` position pre-1.0).

### Changed
- a pattern-rule message's interpolation placeholder now renders as `[see: <label>]` rather than `{$self.field}` (#116, vsdd-cli v0.3.0 diagnostic-efficacy review item 8 part 1): the old `{$self.status}` read as an *un-substituted template* (looked like a bug); `[see: status]` reads as a deliberate cross-reference to the labeled quoted block below. The value still rides out-of-line in the prefix-marked `quoted[]` block — never inline in the message (DESIGN §Output; the same forgeable-marking property `trusted:false` protects). The block label drops the `$self.` prefix so it reads as a field name; a collision or repeated reference is numbered (`status [2]`) so each pointer resolves to exactly one block. Per vsdd-cli's ruling relayed 2026-07-29.
- `families` in the `verify --json` envelope is now a **tri-state plus a reason** (#107, vsdd-cli v0.3.0 diagnostic-efficacy review item 4 — "families was unfalsifiable"). Each of the five families reports `{ "state": "active" | "inert" | "inactive", "reason": "..." }`: `active` = data supplied and the check ran this pass; `inert` = configured but did no work (e.g. `vocabulary.yaml` present but `vocabulary_globs` matched no walked file); `inactive` = not configured. The `reason` documents precisely why — resolving the ambiguity where `active`/`inactive` conflated configured-vs-ran (e.g. `vocabulary_globs` set with no `vocabulary.yaml` now reads `inactive` with the clarifying reason). **This reshape (string → object) is the breaking change** that makes the envelope's `1.1.0 → 2.0.0` bump a MAJOR one.

### Fixed
- `verify --json` without `--quiet` no longer also re-renders every finding as human TTY text on stderr (#117, vsdd-cli v0.3.0 diagnostic-efficacy review W4): the machine envelope on stdout is authoritative, so the duplicate stderr rendering (a ~1.7x token cost for an agent capturing both streams, and now that `pipeline_error` is in-envelope, fully redundant) is suppressed under `--json`. Non-`--json` output is unchanged.
- explain-page "See also" references no longer link to `../../DESIGN.md#…`, a path an adopter running the installed binary does not have (and whose `#summary` anchor had drifted for `E0060`) (#117, vsdd-cli v0.3.0 diagnostic-efficacy review W4): they now name the design section in prose (`the mdatron design reference, § …`), so the reference is meaningful without a dead relative link.
- `verify --compact` now budgets a quoted value ahead of the engine message, and never exceeds the 512-byte cap (#115, vsdd-cli v0.3.0 diagnostic-efficacy review item 8 part 2): a finding's quoted **value** block is now retained ahead of the lower-priority message prose, so a message placeholder can no longer outlive the value it points at (a dangling reference under tight budgets); the prose fills whatever budget remains. The same lane closed a pre-existing cap breach (surfaced by the change's cold review): when the essential head landed in [488, 512] bytes with a quoted region present, the first-line elision was appended unconditionally, overflowing to as much as 537 bytes (a `debug_assert` panic in debug, a silently oversized block in release) — the quoted section now opens only when there is room for at least the elision marker. (Item 8 part 1, the placeholder rendering, is tracked separately.)
- a finding no longer advertises `mdatron explain <code>` for a code the binary cannot explain (#113, vsdd-cli v0.3.0 diagnostic-efficacy review item 7): a pattern-rule finding carries an adopter-defined code (e.g. `VSDD-E0201`) that mdatron has no explain page for, yet every form printed `= explain: mdatron explain VSDD-E0201` — a dead-end the same binary rejects. The explain pointer (`explain_ref`) is now emitted only when a page actually resolves; for an unresolvable code it is `null` (the schema already permits null, so no version bump). Corrects the TTY, compact, and JSON forms alike. mdatron-namespace findings are unaffected.
- overlapping `file_globs` entries no longer check the same file more than once (#109, surfaced by the #108 cold review): a file matched by two globs (e.g. `**/*.md` and `docs/**/*.md`) was walked once per matching glob, so every per-file finding was duplicated and `summary.files_checked` over-counted — corrupting the very checked-N-vs-checked-nothing signal #105 restored. The governed-file list is now deduped by root-relative path; the dead-glob warning (#108, `MDATRON-W0046`) is unaffected (a glob that only re-matches an already-collected file is redundant, not dead).
- `summary.files_checked` now reports the true number of files **validated** this run, not a count of files that produced findings (#105, vsdd-cli v0.3.0 diagnostic-efficacy review item 1). The old v0.1.0 stub counted unique files referenced in findings, so a clean run over N files reported `0` — indistinguishable from an empty jurisdiction. The count is threaded from `verify` (`VerifyReport.files_checked`); incremental runs count the scope. Field type unchanged (integer); the published output schema gains a `description`. Closes the core of the "checked N vs checked nothing" audit-signal gap.

### Added
- `mdatron explain --list` enumerates the diagnostic-code catalog (`code — summary`, sorted), and `mdatron explain` now accepts a **short code** — `E0050` resolves to `MDATRON-E0050` (#117, vsdd-cli v0.3.0 diagnostic-efficacy review W4): an operator can discover codes without a full code in hand and need not paste the `MDATRON-` prefix. The listing is sourced from the golden `schema/code-catalog.json`, so it cannot drift from the contract. README now points at the published output schema and these commands (schema discoverability, W4).
- quoted adopter content is marked untrusted in the `verify --json` envelope (#114, vsdd-cli v0.3.0 diagnostic-efficacy review item 9): every `quoted[]` region now carries `origin: "adopter"` and `trusted: false` alongside `label`/`content`, so a machine consumer (an agent) reads the untrusted status in-band instead of inferring it from the TTY-only `> ` prefix convention. The marking is a **serialize-time constant of the quoted-region type** — emitted centrally, not from the ~two dozen construction sites — so no code path can emit adopter content marked trusted and a new site cannot forget it. **Adding required fields (`origin`/`trusted`, both `const`) under a closed object is breaking** — part of the envelope's `1.1.0 → 2.0.0` major bump. DESIGN § Output records the marking.
- `pipeline_error` in the `verify --json` envelope (#112, vsdd-cli v0.3.0 diagnostic-efficacy review items 5 + 6): on a failed pipeline the envelope now carries `pipeline_error: { code, kind, message }` — present only when `pipeline_status` is `failed`, omitted on success. **Item 6:** the failure reason previously lived only in the stderr `= note:`, which `--quiet` suppresses, so `--json --quiet` (the documented CI mode) received `findings: []` with no cause; it now rides in-band and survives `-q`. **Item 5:** `kind` (`config` | `io` | `schema_load` | `pattern_load` | `glob` | `frontmatter` | `index_build` | `expr_parse` | `eval`) disambiguates the ~9 senses the single `MDATRON-E0080` code conflated, so a consumer can branch on the failure class without parsing prose. Additive (a new optional field), folded into the envelope's `1.1.0 → 2.0.0` bump for 0.4.0. `E0080`'s explain page now documents the field (and drops the stale "v0.1.x init will scaffold" framing).
- `MDATRON-W0047` schema-dir-missing (#110, vsdd-cli v0.3.0 diagnostic-efficacy review item 2): an absent `.mdatron/schemas/` directory left the schema family (Layer 1) unable to run, so a file declaring a `schema_class` passed unchecked and the run exited 0 "clean" — a false-clean only `families.schema:inactive` hinted at. It now warns when the schemas dir is missing **and** a walked file declares a `schema_class` that nothing serves (no schema, no rule context). The project-level companion to `MDATRON-W0045`, it fills exactly the gap W0045 leaves (W0045 stays silent when there is no validation infrastructure at all, to preserve no-adopter-data-clean). A Schematron-only project — a `schema_class` served by a rule `context:` — is not nagged; a present-but-empty dir is a deliberate opt-out; both dirs absent still hard-fails as before. `verify_file` now returns whether the file's `schema_class` was unrouted (threaded to `run`).
- `MDATRON-W0046` jurisdiction-glob-matches-nothing (#108, vsdd-cli v0.3.0 diagnostic-efficacy review item 10): a `file_globs` entry in `.mdatron/config.yaml` that matches **no** file silently shrank the checked corpus — `verify` walked fewer files than the declared jurisdiction and still exited 0, indistinguishable from a clean slice. Each dead glob now warns (once per pattern, on a whole-tree run only — an incremental run legitimately sees part of the tree), naming the pattern as quoted adopter text so a typo or a stale path is caught. The file-level twin of `MDATRON-W0043` (a dead `vocabulary_globs`); together with `summary.files_checked` (#105) it closes the "narrowing `file_globs` silently shrinks the corpus" leg of the audit-signal gap.
- `MDATRON-W0045` schema-class-unrouted (#106, vsdd-cli v0.3.0 diagnostic-efficacy review item 3): a file declaring a `schema_class` that matches **no** schema **and no** rule context was silently unvalidated — nothing validated it, yet the run reported clean. It now warns, so a typo'd or unregistered class on an unchecked file is loud. Fires only when validation infrastructure exists (some schema or pattern), preserving the no-adopter-data-is-clean property; a class routed by a schema or any matching rule stays silent.
- confined no-follow read of the governed-file body (#103, partial): the per-file check reads governed files once through `confine::open_confined` (openat/`O_NOFOLLOW`) rather than raw `std::fs::read_to_string`, so a symlinked governed file is refused (`MDATRON-E0012`) instead of followed. A capture-complete seam makes the body snapshot's immutability testable. The full immutable-snapshot unification (index/pin/citation) remains tracked on #103.
- incremental verify (#102): `mdatron verify --changed <file>` verifies the changed file plus its transitive dependents (#100) against full cross-file context, reporting the same findings a whole-tree run would for those files; a `.mdatron/` change forces whole-tree, and any path that doesn't resolve to a governed file fails safe to whole-tree. The visited-file scope prints to stderr.

## [0.3.0] - 2026-07-29

### Added
- dsl: `filter(x in arr, pred)` construct (#93, first-consumer raise #73; the count-with-predicate gap the 2026-07-28 falsifiability run recorded, now closed) — returns the sub-array of elements matching the predicate, composing with `count`/`len` for exactly-N / at-least-N arity rules the boolean quantifiers cannot express directly (`count(filter(m in $self.members, $m.kind == "lane")) == 1`). A narrowed-inventory addition (frontmatter arrays, not body content — outside the L85 body-content gate); confirmed by a focused cold-authoring run (the previously-inexpressible exactly-one rule authored confidently in one pass). DESIGN construct inventory + docs/dsl-reference.md amended; the falsifiability report's known-limit marked closed
- agnosticism-audit residuals mechanized (#91, v0.3.0 rank-1 cluster; DESIGN § Validation is data-driven acceptance cluster): a methodology-vocabulary denylist sweep over engine-authored text (production source + explain pages) — which caught and fixed two real leaks (a `domain-prompts` example in the index module doc and the E0094 page's vsdd-specific numeric example, both now agnostic); a dependency-manifest allowlist requiring a `docs/dependencies/<crate>.md` record per dependency — which caught three real gaps (added `saphyr` + `libc` records, replaced the stale `serde_yaml` record with `serde_yaml_ng` after the #69 migration); a no-adopter-data run asserting all families report inactive (not a panic); and a symlink-cycle fixture proving the no-follow extras scan terminates. All four run in CI
- envelope contract-stability + per-verify family activity (#90, first-consumer rank-1 ask; envelope version 1.0.0 -> 1.1.0): `mdatron verify --json` now carries a `families` object reporting each of the five check families as `active`|`inactive` — active meaning data was supplied and the family ran this pass (invoked, not fired), so a consumer can audit that a required family was actually invoked; the same field satisfies DESIGN's 'the inactivity is reported' criterion. A machine-readable envelope schema is published at `schema/mdatron-output.schema.json` (draft 2020-12, strict) for consumers to pin and validate. Contract-stability tripwires in CI: the emitted envelope validates against the published schema (shape-change fails without a schema update), schema version tracks `OUTPUT_VERSION` in lockstep, the three output forms agree on findings, every production code literal resolves in the explain catalog, and a committed code-semantics golden (`schema/code-catalog.json`) fails on any code-meaning change without regeneration
- config: `vocabulary_globs` scopes the vocabulary family (#97). A scope list beside `require_frontmatter`: when set, the naming register (`E0090`–`E0094`) applies only to matching files, so a live spec estate is governed while a historical archive stays walked and routed but unscanned; empty (or absent) falls back to every walked file. mdatron dogfoods it — the vocabulary family now governs its own `docs/methodology*.md` while the review-log (a frozen archive of a retired handle scheme) stays out of the register
- vocab: `MDATRON-W0043` vocabulary-scope-matches-nothing (#98) — a present-but-zero-match `vocabulary_globs` reports loudly, mirroring the route family's coverage loudness, so a mistyped or stale glob cannot silently disable the whole register
- vocab: `MDATRON-W0044` vocabulary-term-status-conflict (#95) — a term declared both `registered` and `draft` resolves to draft (the permissive status) with a warning, implementing the DESIGN § Validation-is-data-driven conflict-outcome criterion (the #91 residual); the conflicting term rides a quoted region
- dev: mdatron now develops itself under the vsdd methodology with mechanized enforcement (#96/#97/#99) on the enforcement-grades model (detection → friction → CI-backed block → tamper-proof): a `commit-msg` amendment-citation guardrail (friction — a governed-DESIGN change cites its ratified review) and the vocabulary family over its own governance docs (CI-backed). Conduct + seams recorded in `docs/methodology.md` and `docs/methodology-enforcement.md`. A session-stop-hook attempt was tried, cold-reviewed, and dropped as grade-2 friction that cannot self-protect — the durable checker-edit catch is CI-tested integrity
- engine: dependency resolver (#100, sub-lane B of incremental verify #92) — the transitive dependent set of a changed file over governance, rule-reference, and shared-key edges, backed by provenance-aware indexing (`Index` now records `key → contributing files` and its source set, with `entries` semantics unchanged). Internal foundation for incremental verification; not yet wired to a CLI mode

## [0.2.0] - 2026-07-28

### Fixed
- dsl: `keys:` array selections now fan out to one index entry per element — the array-of-objects registry shape (promised by the 2026-06-01 falsifiability report's Gap-6 revision) had never landed in code, making the natural registry form unindexable; caught by the #47 cold-context re-run (#88)
- dsl: `let:` bindings evaluate in declaration order and may chain (a later binding referencing an earlier one) — the former BTreeMap storage evaluated alphabetically, breaking declared chains; caught by the #47 cold-context re-run (#89)

### Added
- docs at the shipped surface (#49): README rewritten for the current engine — init-first onboarding (jurisdiction via `config.yaml`, the D1 refusal and `--files` escape, managed-manifest E0060), current diagnostic output shapes (marked quoted regions, `--compact`), a Conformance-families section (routes/pins/vocabulary/citations with worked data examples and their codes), and an explicit init-and-hook adoption sequence ending at self-verification in CI; stale bootstrap-era references retired (monorepo install story, V1-SHIP-CRITERIA pointers, pre-marking output samples). The DSL reference (`docs/dsl-reference.md`) landed with #47. Acceptance reads pending: operator + one external cold reader (vsdd-cli)
- falsifiability re-run at the narrowed scope (#47): a cold-context agent authoring 8 rules (the three 2026-06-01 passers + five vsdd-cli first-consumer rules) from `docs/dsl-reference.md` alone scored **100% one-pass** against the ≥80% bar, measured by a per-rule harness driving the real binary over seeded fixtures — the 2026-06-01 waiver (43%, accepted-without-retest) is discharged. `docs/dsl-reference.md` is new: the complete construct inventory with evaluation semantics (short-circuiting, Null-guards, index fan-out and collision order), doubling as the #49 DSL-reference deliverable. DESIGN's construct-inventory overclaim ("arithmetic and comparison operators" — the engine implements equality only) corrected. Known limit recorded: no filter/count-with-predicate, so "exactly N matching" has no direct encoding
- citation family (#86, fourth and final #45 family): `file:line` and `file:start-end` citations in opted-in artifacts are verified against the working-tree snapshot — uncommitted content counts, no git subprocess. A citation naming absent content is rejected (`MDATRON-E0100`, the 7-of-8-findings-cited-absent-code evidence class); one past the target's end is rejected (`MDATRON-E0101`, malformed ranges included); citation paths are held to the confinement contract (parent/absolute escapes refused intact under `E0010`/`E0011`, symlinked targets `E0012`). Activation is per-route via the new optional `citations: true` route flag (format amendment recorded on #86 and in DESIGN): living corpora opt in, historical corpora — whose citations were true when written — stay archival. Two explain pages
- vocabulary family (#85, third #45 family; includes the D3 numeric-claims member from first-consumer raise #79): `.mdatron/vocabulary.yaml` drives the prose scan over governed artifacts — unregistered bold-introduced coinages flagged (`MDATRON-E0090`, draft-status exempt; the bootstrap coinage-log discipline mechanized), letter-plus-number clusters outside the allowlist flagged (`MDATRON-E0091`, the letter-cluster incident's mechanization), reserved-word use surfaced (`MDATRON-E0092`), listed register anti-patterns flagged (`MDATRON-E0093`), and prose numerals restating a configured frontmatter field's count and drifting from it flagged (`MDATRON-E0094`, digits and word-numbers, configured field references only). Adopter patterns compile on the linear-time engine; matched prose rides quoted regions at precise source lines; each registry section activates independently; absent registry = family inactive. Five explain pages. DESIGN §Five-families vocabulary sentence amended to record the ratified numeric-claims member
- pin family (#84, second #45 family; mechanizes the 2026-07-27 staleness incident's attention loop): `.mdatron/pins.yaml` — governing documents pin sha256 over the files they govern. A governed-file change with a stale pin fails (`MDATRON-E0061`, both edge ends quoted); an unopenable target fails (`MDATRON-E0062`); re-pin is one command (`mdatron pin --update`, with `--dry-run`; check mode without flags). Un-pinning persists as a justified `unpinned:` tombstone — the standing `MDATRON-L0001` lint fires from it on every whole-tree run, an unjustified entry flags `MDATRON-W0042` (the DESIGN governance-weakening annotation form, now recorded). Confinement escapes drop fail-closed under the confinement codes; absent `pins.yaml` = family inactive. Four explain pages. mdatron dogfoods: DESIGN.md pins `src/codes.rs` (the incident fixture, live in CI) and the init manifest (closing DESIGN §Governance's fixed-point anchor)
- route family (#83, first of the #45 conformance families; formats ratified 2026-07-27): `.mdatron/routes.yaml` is a closed-world allowlist over the governed tree — with route data supplied, an unclaimed walked file blocks (`MDATRON-E0030 unrouted-file`), a route citing an absent governing document blocks (`MDATRON-E0031`, no-follow existence check), two routes claiming one file is an error (`MDATRON-E0032 route-conflict`, the ratified conflict outcome), and a filename underivable from the route's `naming` grammar is flagged (`MDATRON-W0041`, linear-time regex via new zero-dependency `regex-lite`). Confinement-escaping entries (parent-segment/absolute, existent and absent targets) are rejected under the path-confinement codes and dropped fail-closed; absent `routes.yaml` leaves the family inactive. Four explain pages; mdatron's own corpus routed (`review-log/**` governed by DESIGN.md with the slug-incident naming grammar). DESIGN open-question blocks reconciled in-lane per the #87 step: data formats recorded resolved, code-range block updated with the #50/#80 resolutions, mutation floor carved into its own OPEN block
- compact verify output (#44, #80 D4): `mdatron verify --compact` emits the agent-context form on stdout — one block per finding, blank-line separated, hard-capped at the ratified 512-byte per-finding contract limit (recorded in DESIGN §Output and docs/cost-ledger.md). Engine-authored head line (`E[CODE] file:line:col summary — message`); quoted adopter content rides prefix-marked beneath (`=label:` + `> ` lines via the partition renderer); truncation drops whole lines from the tail and closes cut regions with an engine elision marker — never mid-line, never mid-escape; an oversized head elides at a char boundary. Pipeline failures render compact too. Measured real-corpus findings: 315/246 bytes
- verify jurisdiction loudness (#82, executing #80 rulings D1+D2): an absent `.mdatron/config.yaml` now REFUSES — `no jurisdiction declared` pipeline failure, exit 2 — instead of silently walking `**/*.md`; jurisdiction is always explicit (an explicit `--files` run declares it on the command line and needs no config). New opt-in config key `require_frontmatter` (globs): a matching file that parses to no-frontmatter fires `MDATRON-W0040 governed-file-has-no-frontmatter` (warning) with an explain page, closing the loud-failure/silent-absence asymmetry from the fence-edge consumer raise; mdatron's own config enables it for `review-log/**/*.md`

## [0.1.0] - 2026-07-22

### Removed
- bootstrap: retired the throwaway `bootstrap-tooling/` (`bootstrap-validator.py` + `canonical-counts.yaml` + `live-entities.yaml`) per its own documented exit criterion — "deleted when `mdatron-core verify` runs cleanly against the project" (BOOTSTRAP-MITIGATION.md § Mitigation 1). That criterion is now met: `mdatron verify` runs clean on the repo and is enforced by the `self-validate` CI gate. This is the bootstrap-*validator* deletion, which by design predates the full bootstrap-*period* exit (the remaining criteria — v1.0 on crates.io, vsdd-cli Step 5, coinage-log processed — track under #48). (#46)

### Fixed
- frontmatter: a UTF-8 BOM or CRLF fences no longer read as no-frontmatter — previously a BOM'd/CRLF'd governed file silently passed the walk (parse absence is indistinguishable from not-governed) while lenient consumer-side readers validated it, a split-brain hole on Windows-editor provenance. `parse()` now strips one leading BOM and tolerates CRLF fences; the shared fence-edge conformance corpus from the first external adopter (BOM opening, CRLF fences, bare `---` inside a block scalar pinned as deliberate shared truncation semantics, empty block, unterminated fence) lands as tests, and E0050 source-line resolution survives BOM+CRLF (consumer raise, #78)
- verify: the committed `.mdatron/config.yaml` `file_globs` are now honored as the consumer-authored jurisdiction — previously `VerifyConfig::new` hardcoded `**/*.md` and the config was inert, so mdatron walked (and could refuse) third-party markdown it has no mandate to govern, e.g. chassis-deployed command files; live incident: a crosslink-deployed command file with non-strict-YAML frontmatter blocked a consuming estate's commits. `--files` still overrides for ad-hoc runs; a present-but-malformed config fails the pipeline loudly instead of silently falling back. `config.yaml` is demoted from the managed partition to a *seed* (written once, adopter-owned, never drift-refused) with a demotion tombstone per DESIGN § Governance data is governed — manifest v2 carries tombstones, v1 manifests still listing it as managed are honored as data (drift-checked, engine-repaired). mdatron's own config now scopes verification to `review-log/**/*.md` (consumer raise, #77)
- test(confine): `confine_ops_do_not_leak_fds` was flaky under parallel execution — it took a single `/proc/self/fd` snapshot (process-global, shared across threads) and tolerated only +2, so descriptors briefly held by unrelated concurrent tests failed it despite zero leak. Now min-samples the count to filter transient churn and tolerates bounded parallel noise (32) while staying an order of magnitude below the per-op leak signal (300); a deterministic red-gate test reproduces the race (#75)
- Path-confinement helper: fallback and starts_with flaws; traversal test passes only via macOS symlink (L2)

### Security
- output: adopter-derived text no longer renders inline in engine-authored diagnostic lines (a `DESIGN.md` § Output falsifier and the agent-context injection surface). The failing document value / unexpected keys of a schema violation (`MDATRON-E0050`) and interpolated `{{expr}}` values in DSL rule messages are now carried structurally separate (a distinct JSON envelope field; prefix-marked `= found:` / `= unexpected:` blocks in TTY) via a partition renderer — split set (LF/CR/VT/FF/NEL/U+2028/U+2029) becomes prefixed breaks, escape set (remaining control chars incl. FS/GS/RS) becomes inert `\xNN`, and every line is prefixed so adopter bytes cannot forge an end-of-quote. E0050 messages are reconstructed from the schema-side violation kind rather than jsonschema's value-embedding `Display` (#76)
- index: complete the diagnostic marking discipline at the extract/parse stage — Parse/Selection/UnsupportedFileType/read-Io messages now escape adopter path text (control bytes render as inert `\xNN`), consistent with the confinement-stage variants (#67)
- index: glob enumeration semantics unpinned — closed-world discipline not enforced or tested; replaced glob::glob with an engine-owned no-follow bounded walk (absorbs #54: glob metacharacters in the project-root path parsed as pattern syntax, and non-UTF8 roots degraded before matching) (#55)
- confine: open_confined silently sanitizes non-Normal components instead of rejecting them (#53)
- Competent-demotion refusal asserts an engine signal the engine cannot emit (L41)
- Demotion falsification and seed conflate pin drift with annotation refusal (L39)
- Demotion annotation has no specified home (L38)
- Competent manifest demotions are silent; the loud bar fails for removals (L37)
- Seeds exercise only the split leg (L36)
- Completeness lemma false: consumers split on FS/GS/RS (escape set) (L35)
- Budget scope over dependent-closure traversal ambiguous (L25)
- Partition demotions escape the loud-governance channels (L24)
- Manifest self-hash is a fixed point; the anchor must be stated (L23)
- Compact truncation vs marking invariant unspecified (L22)
- Escaping and line-splitting cannot share one alphabet; a partition is required (L21)
- Handle confinement component-wise unstated (L11)
- Governance weakening is grep-loud only (L10)
- Init manifest self-pinning unstated (L9)
- Annotation owner unauthenticated; annotation text unmarked channel (L8)
- Extras scan bound dimension unnamed; no real-tree fixture (L7)
- Aggregate snapshot memory and concurrent count unbounded (L6)
- Escaping and line-splitting alphabet excludes Zl/Zp separators (L5)
- Inline interpolation does not fit block prefix marking (L4)

### Added
- self-validation: mdatron now verifies its own typed-markdown corpus (`review-log/*.md`) against a committed `.mdatron/` config — the self-hosting gate. Ships a `review-entry` JSON Schema (draft 2020-12) validating the 12 required review-entry frontmatter fields, an `mdatron init`-deployed managed partition, and a blocking `self-validate` CI job that fails on any schema violation. Retires the throwaway bootstrap validator (see Removed). (#46)
- diagnostics: `MDATRON-E0050` frontmatter-schema-violation now reports the violation's precise source line (`--> file:line`) instead of the frontmatter block start, resolving the schema JSON pointer via an error-path-only position-tracking parse — directly actionable for the fixing agent (#65)
- CI: GitHub Actions gauntlet — fmt + clippy (`-D warnings`), workspace tests on Linux, macOS, and Windows, and `cargo audit`, all on the pinned MSRV toolchain (crosslink/Thermite-shaped) (#68, #64)
- docs(README): pre-commit integration guide recommending a fail-closed wrapper (block the commit when the `mdatron` binary is missing, rather than skip silently), from the first external-adopter field report (#60)
- Design increment: conformance-check families (phase 1a cycle) (#1)

- `mdatron explain CODE` v0.1.0 baseline catalog — five per-code prose pages
  embedded via `include_str!` (MDATRON-E0001, E0002, E0050, E0070, E0080); each
  page carries the four required structural elements (`**Severity:**`,
  `**Introduced in:**`, `## What this means`, `## How to fix`) per
  crosslink #13 Phase 1a behavioral spec
- TTY `= explain: mdatron explain <code>` line in rustc-shape diagnostic
  output when the finding's `explain_ref` is `Some` — surfaces the explain
  subcommand affordance at the per-finding diagnostic
- `mdatron/README.md` — first-author README per DR-F1: install + first run
  + Layer 1 schema example + Layer 2 pattern example + relationship to vsdd
  + where to go next; TRON-blockchain disambiguation in section 1 per TW-F3
- `mdatron-cli/tests/cli_integration.rs` — 18 CLI integration tests
  covering the `explain` subcommand (5 baseline + unknown + foreign-
  namespace), the rendered `= explain:` line, README presence + structure
  + round-trip, and `--quiet --json` flag-combination drive-by

### Changed
- workspace collapse (#81): the two-crate workspace (`mdatron-core` + `mdatron-cli`) is now a single `mdatron` crate, executing the 2026-06-02 binary-first directive per the operator ruling of 2026-07-22 — mdatron is consumed **as a binary** (`--json` is the machine interface); the crate's lib target is an implementation detail for its own tests (unit suites, integration suites, the load-bearing `compile_fail` confinement doctest) and carries **no API-stability promise**. DESIGN §Summary + ledger item 2 amended to record execution. Consumers linking `mdatron-core` in-process must move to shelling out to the binary (vsdd-cli coordination sequenced via #81)
- docs: the E0012 explain page and DESIGN.md § Verification is fast qualify the swap-proof confinement guarantee as Unix-only (`openat`/`O_NOFOLLOW`); the non-unix fallback's weaker check-to-open window is stated as a tolerated, carved-out posture until a handle-based walk lands (#56)
- ops: repair the hollow chassis install (crosslink init --force) (#58)
- mdatron cleanup: truth reconciliation + chassis onboarding (layer 0, pulled forward) (#2)
- Spec-review round 7 (conformance-engine rev 7, confirmation: the claim reduction) (L7)
- Spec-review round 6 (conformance-engine rev 6, final: security, two paragraphs) (L6)
- Spec-review round 5 (conformance-engine rev 5, final targeted: security) (L5)
- Spec-review round 4 (conformance-engine rev 4, targeted) (L4)
- Spec-review round 3 (conformance-engine rev 3) (L3)
- Revision line and evidence base stale (L33)
- Compact output not pinned to the verify command (L32)
- Reverse rule-reference direction unfixtured (L31)
- Trace-escaping falsification unowned by any criterion (L30)
- Mutation fixtures need a declared test seam (L29)
- Weakening lint universality contradicts incremental soundness (L28)
- Alphabet seeds omit Zp (U+2029) (L27)
- Concurrent-invocation-count bound has no exceedance seed (L26)
- Fixture-corpus completeness clause is partly circular and unaudited (L19)
- Agnosticism scope word documentation unqualified (L18)
- Normal-case performance rests on measurement without threshold (L17)
- Milestone conversion criterion omission is silent (L16)
- Governance-edge dependent propagation unfixtured; reverse direction unexercised (L15)
- Conflicting-data precedence outcomes unasserted (L14)
- Concurrency criterion passes trivially without in-flight mutation (L13)
- Snapshot property has no mutation-in-flight falsification (L12)

- `Finding::format_tty` consolidated to the rustc shape: first line is
  `<label>[<code>]: <summary>` (was `: <message>`); `<message>` now flows
  through a `   = note: <message>` line. The rendered output is closer
  to rustc / clippy convention. `mdatron-cli/src/main.rs:print_finding`
  delegates to `format_tty` as single source of truth (Phase 2c
  consolidation per crosslink #13)
- `cmd_explain`'s not-found message now points to DESIGN-MDATRON.md §
  Reserved mdatron codes for the structural meaning of unimplemented
  codes (crosslink #13 Phase 4 Convergence 6: SC F1 + UX F4 + AIE F6
  + DR F4)

### Methodology notes

- Phase 2 of the binary-first refactor honors the SO disposition recorded
  2026-06-02 at `vsdd-cli/docs/refactor/phase-0-output-format/DESIGN.md` open
  question #2: implement explain for v0.1.0; retain the `= explain:` line.
  This reverses DR-F2's earlier "strip the line" finding
- `binary-first-plan.md` row 14 amended in this cycle to reflect the
  disposition reversal (was "Strip"; now "Implement explain catalog +
  retain line")
- `OperatorDirectiveApplied{directive: phase-2-explain-disposition-honored}`
  captured at this commit per the M5 F3+F9 audit-trail-on-act discipline;
  prose record at `vsdd-cli/docs/refactor/phase-2-mdatron-json/phase-1a-
  behavioral-spec.md § Operator-directive housekeeping`
- crosslink #13 cluster-batched Phase 3 cold-session: 18 reviews
  (`review-log/2026-06-07-<domain>-phase-2-bundle.md`); ~137
  findings; 0 hallucinations; 4 honest domain-wide dismissals
  (Accessibility, Localization, Privacy, Performance Engineer-mostly)

## [0.0.1] - 2026-06-01

### Added

- Cargo workspace structure with `mdatron-core` library + `mdatron` binary crates per [BOUNDARY-PREAMBLE](./BOUNDARY-PREAMBLE.md) § 2
- `mdatron-core::diagnostic` — `Finding`, `Severity`, `Location` types with rustc-style TTY formatting (`format_tty` matches `severity[code]: message\n  --> file:line[:column]` convention; optional `= help:` and `= explain:` lines)
- `mdatron-core::error` — internal `Error` enum (IO / YAML / JSON / Config variants) via `thiserror`
- `mdatron-core::frontmatter` — YAML frontmatter parser handling typical, empty (`---\n---\n`), malformed-YAML, no-closing-marker, and body-internal-dashes cases
- `mdatron verify --files <FILE>...` — CLI subcommand parsing frontmatter from given files; reports per-file count + rustc-style diagnostics on failure
- `mdatron explain <code>` — stub (returns "extended docs not yet implemented at v0.1.0")
- `rust-toolchain.toml` pinning channel 1.85 + `clippy` + `rustfmt` components per Rust supplement Platform Engineer extensions
- `rustfmt.toml` declaring `edition = "2021"` + `max_width = 100`
- `docs/dependencies/{serde,serde_yaml,serde_json,thiserror,clap}.md` — retroactive VSDD-E0100 investigation entries for the 5 workspace deps
- `.vsdd/events/` bootstrap-period audit-trail entries for Phase 2a + Phase 2b composition declarations (canonical event-log analog per BOOTSTRAP-MITIGATION § Mitigation 3)
- AIE domain review on mitigation techniques at [`review-log/2026-06-01-ai-engineer.md`](./review-log/2026-06-01-ai-engineer.md)

### Methodology notes

- Phase 2a Red Gate authored before implementation (14 failing tests + 1 passing legitimate test); Phase 2b implementation closes all 14 (Red → Green)
- M1 inline-multi-domain composition recurrence #7 acknowledged; vsdd-cli Step 2.3 amendment with cost-rubric is the load-bearing resolution path (AIE-MITIG-F6)
- AIE-S3-F1 (compact output format with pinned token budget) deferred to v0.0.x once compact format is implemented
- AIE-MITIG-F4 commit-message convention for sub-agent cost capture: applied prospectively from Phase 2a commit onward
- Bootstrap-period state: `mdatron-core verify` not yet operational against this repo; structural drift caught by `bootstrap-tooling/bootstrap-validator.py` per BOOTSTRAP-MITIGATION § Mitigation 1

### Deferred

- Layer 1 JSON Schema validation (Phase A.1 next iteration) — needs `jsonschema` dep approval
- Layer 2 DSL parser + evaluator + standard library (Phase A.1)
- 5 built-in patterns (Phase A.3)
- Error catalog loader + ~30 seeded codes (Phase A.2)
- SARIF / JSON / compact output formats (Phase B)
- Cross-file index lifecycle (Phase A.1)
- Anchor-ID derivation utility (Phase A.1)
- Bypass-marker parsing (Phase A.1)
- `mdatron registry`, `mdatron init`, `mdatron schema`, `mdatron skill-preamble`, `mdatron verify hook`, `mdatron verify test-error-catalog` subcommands (Phase B+)
- PostToolUse hook configuration template (Phase C)
- `mdatron lsp-serve`, `mdatron mcp-serve` — namespaces reserved; implementations in v1.1 per [V1-SHIP-CRITERIA](./V1-SHIP-CRITERIA.md)
